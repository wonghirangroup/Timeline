---
description: Impeccable design skill — ใช้ /impeccable <command> เช่น critique, polish, colorize, layout, audit, animate, typeset, clarify, harden, adapt, live
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, WebFetch
---

อ่านไฟล์ reference ที่ตรงกับ subcommand ที่ผู้ใช้พิมพ์:

1. ดู argument ที่ผู้ใช้ส่งมา (เช่น `critique`, `polish`, `colorize`, `layout`, `audit`, `animate`, `typeset`, `clarify`, `harden`, `adapt`, `live`, `init`, `document`, `bolder`, `quieter`, `shape`, `craft`, `distill`, `delight`, `overdrive`, `brand`, `onboard`, `optimize`, `extract`, `codex`)
2. อ่านไฟล์ `.claude/skills/impeccable/reference/<subcommand>.md`
3. อ่าน `PRODUCT.md` และ `DESIGN.md` ที่ root project ก่อนทำงานทุกครั้ง
4. ทำตาม instructions ในไฟล์ reference นั้นทุกขั้นตอน

ถ้าไม่มี argument หรือพิมพ์แค่ `/impeccable` ให้แสดงรายการ commands ทั้งหมดที่มีพร้อม description สั้น ๆ

Project root: `C:\Users\User\Downloads\timeline\timeline`  
Skills path: `.claude/skills/impeccable/reference/`
